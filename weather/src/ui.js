let ui = {
  render(datas) {
    let elements = datas.map(data => {
      let {
        dt_txt,
        temp_min,
        temp_max,
        wind_speed: data.wind.speed,
        rain
      }
    });
  }
}

export default ui;
